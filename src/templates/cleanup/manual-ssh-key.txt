ssh-keygen -t rsa -C "__EMAIL__"
# Creates a new ssh key, using the provided email as a label


Add passphrase - this is important to maintain security



User permission on server:

chmod 700 /home/__USERNAME__/.ssh
